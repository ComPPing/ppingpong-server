
spring:
  datasource:
    url: jdbc:mariadb://aws-ppingpong-mariadb.cutzpg5b9okf.ap-northeast-2.rds.amazonaws.com:3306/ppingpongdb # jdbc 시작
    driver-class-name: org.mariadb.jdbc.Driver
    username: rnwnsgud
    password: rnwnsgud1234
  h2:
    console:
      enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      '[hibernate.default_batch_fetch_size]': 100
      '[hibernate.format_sql]': true
    show-sql: true
  output:
    ansi:
      enabled: always

  mvc:
    pathmatch:
      matching-strategy : ant_path_matcher


logging:
  level:
    '[co.kr.ppingpong]': DEBUG
    '[org.hibernate.type]': TRACE